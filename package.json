{
  "name": "chatting-app-server",
  "scripts": {
    "start": "nodemon index.ts",
    "set-port": "npm config set chatting-app-server:port"
  },
  "config": {
    "port": "8080"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "dotenv": "^16.0.1",
    "socket.io": "^4.5.1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.4",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.0"
  }
}
